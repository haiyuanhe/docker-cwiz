<configuration>
    <property>
        <!-- This is the total number of copies. -->
        <name>dfs.replication</name>
        <value>2</value>
    </property>
    <property>
        <!-- This can be a comma-separated list of directories. -->
        <name>dfs.datanode.data.dir</name>
        <value>file://<:data_root:>/hadoop/datanode/</value>
        <description>DataNode directory for storing data chunks.</description>
    </property>

    <property>
        <!-- This can be a comma-separated list of directories. -->
        <name>dfs.namenode.name.dir</name>
        <value>file://<:data_root:>/hadoop/namenode/</value>
        <description>NameNode directory for namespace and transaction logs storage.</description>
    </property>
</configuration>
